# v13 修改说明

1. 病例知识库页的一键删除按钮缩小并右侧对齐，文案改为“删除当前结果”。
2. 文章投喂页支持输入后自动保存到 `data/articles.json`，批量导入不再弹出二次询问。
3. 回答中的文献提示改为中文概括，不再照抄文献长段落；对话框只保留简短提示，详细内容通过文献卡片跳转查看。
4. 文献卡片进一步压缩展示内容，仅显示标题、来源、命中关键词、简短可借鉴方向和详情入口。
