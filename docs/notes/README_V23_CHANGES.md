# v23 修改说明

1. 已把本轮上传的 `知识库（一）(2).xlsx` 复制为项目默认病例库 `data/knowledge_base.xlsx`，并生成 `data/knowledge_base_manifest.json` 记录数据库摘要。当前默认库读取为 77 例：USCC 39 例、其他类型尿道肿瘤 19 例、阴茎癌 10 例、其他 9 例。
2. 除“选择患者后的第一次总体分析”以及医生明确要求相似病例/文献/治疗讨论外，后续普通追问不再自动附加病例和文章卡片，改为调用后台 AI 按问题本身回答。
3. 文章投喂页布局调整：选择文章文件夹与一键分析文件夹移动到选择文件和保存手动输入文章之间。

注意：若本地仍显示旧数据，请确认解压的是 v23 文件夹，并按 Ctrl+F5 强制刷新；长期投喂数据仍保存在用户目录 `.uscc_scc_flask_data`，不会因前端更新丢失。
